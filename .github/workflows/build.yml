name: Kaizen VPN

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          submodules: 'recursive'

      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: 'adopt'
      - name: Set up Flutter
        run: |
          curl -o flutter_install.sh https://raw.githubusercontent.com/BCW52/Kaizen/main/install_flutter.sh
          chmod +x flutter_install.sh
          ./flutter_install.sh
          flutvar=`flutter --version`
          echo $flutvar
          TEST=$(pwd)
          echo $TEST
          cd /home/runner/snap/flutter/common/flutter
          git checkout 2.10.5
          flutvar=`flutter --version`
          echo $flutvar

        working-directory: ${{ github.workspace }}
        env:
          FLUTTER_HOME: ${{ github.workspace }}/flutter

      - name: Build APK
        run: |
          cd ${{ github.workspace }}/
          flutter clean
          flutter pub get
          flutter doctor -v
          flutter doctor
          rm pubspec.lock
          flutter build apk --release --verbose

        working-directory: ${{ github.workspace }}


      - name: Upload APK
        uses: actions/upload-artifact@v2
        with:
          name: app-debug
          path: build/app/outputs/flutter-apk/app-debug.apk
